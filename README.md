# Бесплатный Telegram-бот по обществознанию

В этой версии:
- 1100 заранее прописанных вопросов;
- готовые правильные ответы;
- готовые объяснения;
- тренировка по 5 вопросов и тест по 10 вопросов;
- раздел «Мои ошибки»;
- статистика;
- серия дней (streak);
- база SQLite, которая создаётся автоматически;
- никаких платных AI API.

## 1. Создай бота в Telegram

Открой @BotFather.

1. Отправь `/newbot`
2. Придумай имя
3. Придумай username, который заканчивается на `bot`
4. Скопируй токен

## 2. Установи Python

Нужен Python 3.10+.

## 3. Установи зависимости

В папке проекта:

```bash
pip install -r requirements.txt
```

## 4. Создай файл .env

Скопируй `.env.example` в `.env`.

Внутри должно быть:

```env
BOT_TOKEN=твой_токен_от_BotFather
```

## 5. Запусти

```bash
python bot.py
```

Пока программа работает на компьютере, бот работает.

## Как добавить свои вопросы

Открой `questions.py` и добавь новый блок:

```python
{
    "id": 26,
    "topic": "Экономика",
    "question": "Текст вопроса",
    "options": ["Ответ 1", "Ответ 2", "Ответ 3", "Ответ 4"],
    "correct": 1,
    "explanation": "Почему этот ответ правильный."
}
```

`correct` начинается с нуля:
- `0` = первый вариант;
- `1` = второй;
- `2` = третий;
- `3` = четвёртый.

## Стоимость

Сам код и запуск на своём компьютере — бесплатно.

Если нужен бот 24/7, понадобится либо:
- держать свой компьютер включённым;
- либо использовать бесплатный тариф хостинга, если подходящий доступен;
- либо позже взять дешёвый VPS.

Для первой проверки идеи платить ничего не нужно.


## Банк вопросов

В `questions.py` находится 300 полностью локальных тренировочных вопросов: по 60 на каждый из пяти блоков — «Человек и общество», «Экономика», «Социальные отношения», «Политика», «Право».

Все варианты, правильные ответы и объяснения уже записаны. Во время работы бот ничего не генерирует через ИИ и не обращается к платным API.

Это авторские тренировочные вопросы, а не официальный банк ФИПИ.


## Встроенная реклама

В проект добавлена картинка `assets/ad.jpg`.

Рекламный блок показывается:
- перед вопросом №1;
- затем перед вопросом №6;
- затем перед №11, №16 и далее через каждые 5 вопросов;
- после последнего вопроса реклама специально не показывается.

Под рекламой есть кнопка на `https://egetraining.com`.

Чтобы поменять частоту, измени в `bot.py`:

```python
AD_EVERY = 5
```

Чтобы заменить рекламу, просто замени файл `assets/ad.jpg`.


## Банк расширен до 1100 вопросов

Теперь по 220 вопросов на каждый блок:
- Человек и общество
- Экономика
- Социальные отношения
- Политика
- Право

Добавлены форматы «верно/неверно», поиск лишнего термина, определение тематического блока,
а также задания на правильные и ошибочные пары «понятие — определение».

Рекламный блок сохранён: перед 1-м вопросом, затем перед 6-м, 11-м, 16-м и далее.


## Полные варианты ответов

Варианты ответа теперь всегда выводятся полностью в тексте сообщения:

A. Полный текст первого ответа

B. Полный текст второго ответа

C. Полный текст третьего ответа

D. Полный текст четвёртого ответа

На inline-кнопках находятся только буквы A/B/C/D. Это сделано специально,
потому что Telegram может визуально обрезать длинные подписи кнопок троеточием.


## Ошибки и отдельный повтор

В меню теперь два разных раздела:

- `❌ Мои ошибки` — статистика ошибок по темам и количество вопросов, которые ещё нужно повторить.
- `🔁 Повтор ошибок` — отдельная тренировка только по ошибочным вопросам.

Логика:
- неправильный ответ автоматически сохраняется;
- повторная ошибка увеличивает счётчик;
- правильный ответ в обычной тренировке не стирает историю;
- правильный ответ в режиме `Повтор ошибок` помечает вопрос как отработанный;
- история ошибки остаётся в SQLite и учитывается в статистике;
- старая `progress.db` обновляется автоматически, удалять её не нужно.
